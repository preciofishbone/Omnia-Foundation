import { OnDestroy, ElementRef } from '@angular/core';
export declare class Focus implements OnDestroy {
    private element;
    private nativeElement;
    omfFocus: Function;
    constructor(element: ElementRef);
    ngOnDestroy(): void;
    private registerEvent;
}
