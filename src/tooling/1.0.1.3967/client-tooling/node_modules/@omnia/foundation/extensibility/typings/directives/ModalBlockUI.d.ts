import { ElementRef, OnInit, OnChanges, OnDestroy } from '@angular/core';
export declare class ModalBlockUI implements OnInit, OnChanges, OnDestroy {
    private element;
    isBlocking: boolean;
    spinSize: string;
    constructor(element: ElementRef);
    ngOnInit(): void;
    ngOnChanges(changes: any): void;
    ngOnDestroy(): void;
    private init;
    private toggleBlockUI;
    private blockModal;
    private unblockModal;
}
