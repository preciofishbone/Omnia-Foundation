import { ElementRef, OnChanges, OnInit } from '@angular/core';
export declare class Show implements OnInit, OnChanges {
    private element;
    private nativeElement;
    show: boolean;
    constructor(element: ElementRef);
    ngOnInit(): void;
    ngOnChanges(changes: any): void;
    private render;
}
