// jQuery interface for SharePoint Taxonomy Picker plugin
import TaxonomyPickerOptions = Omnia.Foundation.SharePoint.Model.ITaxonomyPickerOptions;

interface JQuery
{
    taxpicker: (options: TaxonomyPickerOptions, context, initializeFunc?: () => void) => void;
}