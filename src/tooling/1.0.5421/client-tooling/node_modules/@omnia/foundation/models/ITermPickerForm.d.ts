import { TermStore } from './ITermStore';
export declare module TermPickerForm {
    interface ITermPickerForm {
        termSet?: TermStore.ITermSet;
        terms?: Array<TermStore.ITerm>;
    }
    interface ITermPickerFormOption {
        termSetId?: string;
        isEnterpriseKeyword?: boolean;
        selectedTerms?: Array<TermStore.ITerm>;
        isMulti?: boolean;
        okHandler: (selectedTerms: Array<TermStore.ITerm>) => void;
    }
}
