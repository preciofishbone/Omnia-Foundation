import { ElementRef, OnInit, OnChanges } from '@angular/core';
export declare class Spinner implements OnInit, OnChanges {
    private element;
    spinning: boolean;
    spinSize: string;
    static templateGuid: string;
    private nativeElement;
    private indicatorElement;
    spinType: string;
    constructor(element: ElementRef);
    ngOnInit(): void;
    ngOnChanges(changes: any): void;
}
