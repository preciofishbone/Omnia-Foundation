import { ElementRef, OnChanges, OnInit } from '@angular/core';
export declare class InnerHtml implements OnInit, OnChanges {
    private element;
    innerHtml: string;
    constructor(element: ElementRef);
    ngOnInit(): void;
    ngOnChanges(changes: any): void;
    private render;
}
