import { DefaultColor } from "../../../models";
import { AjaxService } from "./AjaxService";
export declare class DefaultColorService {
    ajaxService: AjaxService;
    private callbacks;
    private defaultColors;
    constructor(ajaxService: AjaxService);
    getDefaultColors: (callback: (result: DefaultColor.IColor[]) => any, useCached?: boolean) => void;
    addOrUpdateDefaultColors: (defaultColors: DefaultColor.IColor[], callback: (result: boolean) => any) => void;
    private clearCache;
    private retrieveDefaultColors;
}
