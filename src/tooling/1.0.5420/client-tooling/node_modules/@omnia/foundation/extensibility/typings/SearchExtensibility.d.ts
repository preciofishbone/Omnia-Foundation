import { Search as SearchModel } from '../../models';
export declare module SearchExtensibility {
    function registerSearchResultProcessor(key: string, searchResultProcessor: SearchModel.SearchResultProcessor, index?: number): void;
    function processSearchResults(items: Array<SearchModel.ISearchResult>): Array<SearchModel.ISearchResult>;
}
