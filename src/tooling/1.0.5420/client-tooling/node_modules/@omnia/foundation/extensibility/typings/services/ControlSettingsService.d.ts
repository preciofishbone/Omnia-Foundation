import { Configurations } from "../../../models";
import { AjaxService } from "./AjaxService";
export declare class ControlSettingsService {
    private ajaxService;
    constructor(ajaxService: AjaxService);
    getControlSettings: (scope: string, controlId: string, siteCollectionUrl: string, siteUrl: string, pageItemId: number, featureResourceId: string, callback: (controlSettings: any) => void) => void;
    addOrUpdateControlSettings: (configuration: Configurations.IConfiguration, callback: (isSuccess: boolean) => void) => void;
}
