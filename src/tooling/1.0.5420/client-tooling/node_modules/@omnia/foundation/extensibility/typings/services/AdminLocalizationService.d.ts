import { LocalizationService } from './LocalizationService';
export declare class AdminLocalizationService extends LocalizationService {
    prefix: string;
    listPrefix: string[];
    constructor(prefix: string);
}
