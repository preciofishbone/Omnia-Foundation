import { Enums } from '../extensibility';
export declare module DocumentIdentifier {
    interface IDocumentIdentifier {
        id: string;
        title: string;
        fileName: string;
        fileExtension: string;
        documentUrl: string;
        serverRelativeUrl: string;
        parentUrl: string;
        listUrl: string;
        webUrl: string;
        webTitle: string;
        lastModifiedTime?: Date;
        lastModifiedBy?: string;
        isNew: boolean;
        status: string;
        documentType?: Enums.DocumentTypes;
    }
    interface IListIdentifier {
        id?: string;
        title: string;
        listUrl: string;
        webUrl: string;
        rootFolderUrl?: string;
    }
}
